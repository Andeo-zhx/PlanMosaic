- [x] 本地 HTTP API 写接口已启用鉴权，未认证请求无法修改日程、历史、配置或审批状态
- [x] `/ui/*` 控制通道与 CLI 桥接请求已统一校验控制 token
- [x] 本地服务默认仅监听 `127.0.0.1`，且未对任意来源返回宽松 CORS
- [x] 所有高风险 POST 接口具备请求体大小上限，并在超限时返回明确错误
- [x] `safeOpenExternal()` 已基于协议与主机做严格白名单匹配，不再使用前缀匹配
- [x] `index.html` 与 `ai-agent.js` 中用户输入、备注、提案内容不再以未转义 HTML 方式渲染
- [x] 页面 CSP 已收紧，关键动态交互不再依赖 `unsafe-inline`
- [x] 敏感配置项（如 `baseUrl`、监听地址、TLS 校验）不能被未授权前端请求直接修改
- [x] proposal 审批仅接受服务端可信标识，不接受客户端伪造完整提案对象
- [x] 未知 proposal 类型、过期提案、篡改提案都会被拒绝，且不会写入脏数据
- [x] 日程、历史和配置写入已统一走受控写入入口，避免并发覆盖写
- [x] 保存与审批关键接口具备 schema 校验，非法结构不会落盘
- [x] Big Task 等关键数据模型字段已统一，读写逻辑不再混用不同命名
- [x] preload 监听器注册与解绑成对实现，不会重复触发或泄漏
- [x] AI 流式请求仅清理本次请求的监听器，不再使用全局性误清理策略
- [x] 保存冲突会排队或明确提示，不会因全局锁直接静默丢失用户操作
- [x] 定时器、动画帧、Blob URL 等运行期资源在切换或销毁后得到释放
- [x] 关键修复具备自动化验证或脚本化验证，且现有桌面端主路径测试无明显回归
