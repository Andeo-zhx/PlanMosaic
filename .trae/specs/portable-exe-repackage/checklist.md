# 检查清单

## 打包结果验证

- [x] 生成的文件名为 `PlanMosaic1.1.0-Portable.exe`
- [x] 只有一个 exe 文件，无其他依赖文件
- [ ] 双击 exe 可以直接启动应用（需要用户手动验证）
- [ ] exe 文件图标显示为 favicon.ico 图标（需要用户手动验证）

## 功能验证

- [ ] 应用启动后界面正常显示（需要用户手动验证）
- [ ] 不同账户登录后数据隔离正常（需要用户手动验证）
- [ ] 数据存储路径为 `%APPDATA%\PlanMosaic\{username}\`（需要用户手动验证）
- [ ] WordMosaic 子应用正常加载（需要用户手动验证）
- [ ] WordMosaic 词书数据正常显示（需要用户手动验证）
- [ ] WordMosaic AI对话功能正常工作（需要用户手动验证）

## 安全验证

- [x] 打包文件中不包含任何 API Key
- [x] 用户首次使用需要配置 API Key

## 性能验证

- [x] 应用启动速度优化（网络测试改为异步执行）
- [ ] 运行流畅，无明显延迟（需要用户手动验证）
