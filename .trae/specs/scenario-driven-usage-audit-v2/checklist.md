# 场景驱动审计 V2 - 验证清单

## 🔴 紧急修复验证 (Tasks 1-5)

- [x] **C1**: 深度规划面板打开后能加载历史会话（`loadDeepPlanningHistory` 被调用）
- [x] **C2**: 深度规划面板关闭后会话数据保存到 localStorage（`saveDeepPlanningData` 被调用）
- [x] **C3**: 深度规划发送消息时调用真实 AI API（非占位实现）
- [x] **C4**: AI 审批提案添加日程后，关闭并重启应用 → 日程仍然存在
- [x] **C5**: AI 添加日程后 `DataManager.saveLocal()` 被调用，localStorage 数据已更新
- [x] **C6**: `collectAll()` 返回的数据体中不含 `apiKeys` 字段
- [x] **C7**: Cloud Sync 上传的数据体中不含明文 API Key
- [x] **C8**: `archivedConversations` 超过 500 条时自动删除最旧条目
- [x] **C9**: `saveHistory()` 在归档超 500 条后仍能正常完成（不因数据量过大而超时）
- [x] **C10**: 用户系统时区为 UTC-5 时，"今天"的日程判断基于 UTC-5 而非 UTC+8
- [x] **C11**: 用户系统时区为 UTC+9 时，启动问候语"早上好/下午好"时段正确
- [x] **C12**: system prompt 中传递的日期与用户系统时区一致

## 🟡 高优先级验证 (Tasks 6-14)

- [x] **C13**: 手动编辑大任务过程中 AI 流式完成 → 两个操作不互相覆盖数据
- [x] **C14**: 同时触发 `saveBigTasks` 和 `refreshScheduleData` → 后完成的操作不覆盖先完成的
- [x] **C15**: Python 后端正在写入 data.json 时，Electron 主进程不并发写入同一文件
- [x] **C16**: 使用 `.lock` 文件机制后，两个写入路径不会产生竞态
- [x] **C17**: 第一条 SSE 流进行中时发送第二条消息 → 第一条的对话记录仍被正确保存
- [x] **C18**: SSE 流在 `result` 事件后断开 → `shouldRefresh` 仍触发日程刷新
- [x] **C19**: 深度规划面板关闭后 document 上的 mousemove 监听器被移除
- [x] **C20**: AI 面板关闭后 ResizeObserver 已断开连接
- [x] **C21**: `config.json` 中 `timeoutMs: -1000` → 加载后使用默认超时值
- [x] **C22**: `config.json` 中 `provider: "openai"` → 加载后回退到默认 provider
- [x] **C23**: `config.json` 中 `model: ""` → 加载后使用默认模型名
- [x] **C24**: AI 返回 `arguments: "not json"` → 工具返回明确错误而非静默执行
- [x] **C25**: `estimated_minutes: "很多"` → 校验失败并返回提示
- [x] **C26**: 磁盘满时保存日程 → 前端显示"保存失败，磁盘空间不足"而非崩溃
- [x] **C27**: Python 写入失败后内存中 `schedule_data` 回滚到写入前状态
- [x] **C28**: 计算机休眠 1 小时后唤醒 → heartbeat 不误触发超时
- [x] **C29**: 页面隐藏时 heartbeat 计时器暂停，恢复时重置（Electron hide/show 事件）
- [x] **C30**: `saveHistory()` 30 秒超时后后续保存调用可正常执行

## 🟡 中等优先级验证 (Tasks 15-23)

- [x] **C31**: 1000 字符的流式回复 → 渲染不随内容增长而变慢（O(n) 而非 O(n²)）
- [x] **C32**: 流式过程中用户手动滚动 → 位置不被强制跳回底部
- [x] **C33**: 打字动画渲染 500 字符消息 → 无明显的布局抖动/卡顿
- [x] **C34**: `requestAnimationFrame` 合并滚动更新 → 滚动平滑
- [x] **C35**: 上传 3 张图片 → Canvas 像素缓冲区被释放，内存未持续增长
- [x] **C36**: localStorage 满时保存深度规划 → 自动清理最旧 session 后重试成功
- [x] **C37**: localStorage 满时 Web 模式归档 → 给用户友好提示
- [x] **C38**: API 返回 429 → 重试延迟为指数增长（1s→2s→4s）+ 随机抖动
- [x] **C39**: API 返回 429 带 `Retry-After: 30` → 等待 30 秒后重试
- [x] **C40**: 连续 5 次 SSE JSON 解析失败 → 前端收到错误通知
- [x] **C41**: 月视图日历点击任意日期 cell → 正确弹出该日期的详情模态框
- [x] **C42**: `postMessage` 的 `targetOrigin` 不为 `'*'`
- [x] **C43**: AI 返回超 50000 字符 → 内容被截断，前端显示警告

## 🟢 低优先级验证 (Tasks 24-29)

- [x] **C44**: data.json 中包含 `"2024-13-01"` → AI 工具不崩溃，跳过该日期
- [x] **C45**: 双击应用图标两次 → 只启动一个实例（第二个被拒绝）
- [x] **C46**: Python 崩溃后 `get-schedule-data` 和 `get-schedule-data-local` 返回格式一致
- [x] **C47**: 同时打开 AI 面板和设置面板 → 按 ESC 只关闭最顶层（设置面板），AI 面板保持打开
- [x] **C48**: 在输入框中按 Ctrl+S → 不触发保存（浏览器默认行为）
- [x] **C49**: Electron 和 Python 创建的备份文件使用相同的命名格式

## 综合回归验证

- [x] **C50**: 完整流程：AI 添加日程 → 手动编辑同一天 → 关闭应用 → 重启 → 两个修改都保留
- [x] **C51**: 完整流程：深度规划对话 30 分钟 → 关闭面板 → 重新打开 → 会话完整恢复
- [x] **C52**: 完整流程：上传图片 + 发送消息 → 流式渲染不卡顿 → 审批提案 → 日程刷新正确 → 关闭面板资源释放
- [x] **C53**: 完整流程：连续使用 7 天 → 归档对话不超上限 → 历史读写性能不劣化
- [x] **C54**: 完整流程：旅行中切换时区 → "今天"判断正确 → AI 理解正确的本地日期
- [x] **C55**: 完整流程：磁盘满 → 提示用户 → 清理空间后操作恢复正常