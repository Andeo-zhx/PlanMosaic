# 场景驱动审计 - 验证清单

## 🔴 紧急修复验证 (Tasks 1-7)

- [x] **C1**: data.json 损坏时，系统 toast 警告用户"数据文件已损坏，已备份"，且不丢失旧备份 ✅
- [x] **C2**: agent_log.json 损坏时，对话面板显示"历史对话加载失败，已备份损坏文件" ✅
- [x] **C3**: `writeScheduleData()` 使用 tmp 文件 + fs.rename 原子写入，写入中途 kill 进程后 data.json 仍完整 ✅
- [x] **C4**: `writeAgentHistory()` 同样使用原子写入模式 ✅
- [x] **C5**: Electron 侧和 Python 侧的备份清理逻辑一致（均按数量+总大小双重限制） ✅
- [x] **C6**: 发送消息时快速双击发送按钮 → 不会重复发送两条相同消息 ✅
- [x] **C7**: 流式接收中网络断开 → `_isSending` 被正确重置，断开后可继续发送消息 ✅
- [x] **C8**: stream 回调内部抛异常（如 DOM 操作失败）→ `_isSending` 仍被 `finally` 释放 ✅
- [x] **C9**: 每个 proposal 生成唯一 ID，存储在消息的 `proposal.id` 字段 ✅
- [x] **C10**: 快速连续确认两个不同提案 → 各自执行正确的提案内容 ✅
- [x] **C11**: 已确认/已取消的提案再次点击确认 → 显示"该提案已处理"而非重复执行 ✅
- [x] **C12**: 账号 A 切换到账号 B → 日历显示账号 B 的日程，而非账号 A 的 ✅
- [x] **C13**: 切换账号后对话面板清空，显示账号 B 的历史对话 ✅
- [x] **C14**: 切换账号后深度规划面板显示账号 B 的规划历史（不显示账号 A 的） ✅
- [x] **C15**: 切换账号后 API Key 配置使用账号 B 的设置 ✅
- [x] **C16**: `saveHistory()` 并发调用时，最后一次调用保存的是完整数据（不丢失中间条目） ✅
- [x] **C17**: AI 回复包含 `<div>` 未闭合标签 → 消息气泡布局不崩溃，内容可读 ✅ — 已修复：先 escapeHtml 再选择性恢复安全标签
- [x] **C18**: AI 回复包含 `<script>alert(1)</script>` → 不执行脚本，纯文本显示或过滤后显示 ✅

## 🟡 高优先级验证 (Tasks 8-14)

- [x] **C19**: 首次启动（无 config.json）→ AI 面板显示"需要配置 API Key"引导卡片，含"去设置"按钮 ✅
- [x] **C20**: 点击"去设置"→ 打开设置面板并自动滚动到 API Key 区域 ✅ — 已修复：添加 scrollIntoView
- [x] **C21**: Python 后端启动失败 → 主面板显示持久 banner"AI 服务未启动，部分功能不可用" ✅
- [x] **C22**: Python 后端重启中用户发送消息 → 返回"服务正在重启，请稍候"而非通用错误 ✅ — 已修复：pythonIsRestarting 检测
- [x] **C23**: Python 后端第 4 次重启失败 → banner 更新为"AI 服务多次启动失败，请检查 Python 环境" ✅ — 已修复：banner 文案 + python-backend-error 暴露
- [x] **C24**: 上传 3000x4000 像素图片 → 自动压缩至 ≤2048px 后编码，base64 ≤ 2MB ✅
- [x] **C25**: 上传 10MB 图片 → 客户端拒绝并 toast"图片过大，请选择小于 5MB 的图片" ✅ — 已修复：toast 文案
- [x] **C26**: 流式接收中关闭 AI 面板 → SSE 监听器被移除，主进程请求被 abort ✅
- [x] **C27**: 流式接收中关闭整个窗口 → SSE 连接被正确关闭，不再向 API 发送数据 ✅
- [x] **C28**: 对话超过约 8000 token → 发送前自动截断历史，toast 提示"对话较长，已截断部分历史" ✅ — 已修复：动态 MAX_TOKENS（flash~8000, pro~16000）
- [x] **C29**: DeepSeek Flash 模型 → SSE heartbeat 45 秒超时 ✅
- [x] **C30**: DeepSeek Pro 模型 → SSE heartbeat 90 秒超时 ✅
- [x] **C31**: 断网后仍可查看日历、编辑日程（纯本地操作） ✅
- [x] **C32**: 断网后点击发送 AI 消息 → 明确提示"该功能需要网络连接" ✅ — 已修复：toast 文案

## 🟡 中等优先级验证 (Tasks 15-24)

- [x] **C33**: AI 尝试创建 500 天周期性日程 → 返回警告"最多支持 365 天，已截断" ✅
- [x] **C34**: AI 批量删除时 dates 数组为空 → 返回提示"未指定要删除的日期" ✅
- [ ] **C35**: 搜索输入"数学"和"数学课"均能搜到"数学课"相关日程（大小写不敏感） ❌ — 搜索 UI 未实现（Task 17 延期）
- [ ] **C36**: 快速连续输入搜索关键词 → UI 不卡顿，最后一次输入后 300ms 才触发搜索 ❌ — 搜索 UI 未实现（Task 17 延期）
- [x] **C37**: 画像生成 3 次失败后配置了有效 API Key → 下次对话达到 10 条后恢复画像生成 ✅
- [x] **C38**: 深度规划 sessions 超过 10 个 → 自动删除最旧的 session ✅
- [x] **C39**: 单个深度规划 session 超过 200 条消息 → 自动截断 ✅
- [x] **C40**: Web 模式归档超过 5 个 → 自动删除最旧的归档 ✅
- [x] **C41**: 手动添加时间槽 "abc-def" → 输入框红色高亮 + 提示"请使用 HH:MM-HH:MM 格式" ✅ — 已修复：添加 .error 类 + 输入时自动清除
- [x] **C42**: 手动添加与已有日程时间重叠 → toast 警告"与 XX:XX 的 XXX 冲突" ✅
- [x] **C43**: 清空对话 → 容器为空 + toast"对话已清空"（不再自动添加"对话已清空"消息） ✅
- [x] **C44**: 中文输入法激活时按 Enter → 不发送消息（仅确认输入法候选词） ✅
- [x] **C45**: 中文输入法激活时按 ESC → 仅关闭输入法候选框，不关闭 AI 面板 ✅ — 已修复：添加 e.isComposing 检查
- [x] **C46**: 设置 API Key 时 Python 后端不可用 → toast"配置已保存，重启后生效" ✅
- [x] **C47**: 启动扫描后打开 AI 面板 → 不出现重复问候（主面板和模态面板各自一条） ✅
- [x] **C48**: 模态面板已有对话时，主面板的启动扫描结果不会被重复添加 ✅

## 🟢 低优先级验证 (Tasks 25-29)

- [x] **C49**: 深度规划模式下 AI 不再调用 `web_search_evaluate` 占位工具 ✅
- [x] **C50**: 周期性添加日程时，被跳过的日期在返回消息中列出 ✅
- [x] **C51**: 同一秒内连续两次保存 → 两次备份文件名称不冲突 ✅
- [x] **C52**: 深度规划长回复（>300 字）直接渲染，不逐字打字动画 ✅
- [x] **C53**: 关闭 AI 面板后再打开 → 之前上传的图片 blob URL 已被释放 ✅
- [x] **C54**: 清空对话后 → 所有 blob URL 被释放 ✅

## 综合回归验证

- [x] **C55**: 完整流程：首次启动 → 配置 API Key → 对话 → AI 添加日程 → 日历刷新 → 确认提案 → 日程生效 ✅
- [x] **C56**: 完整流程：账号 A 登录 → 创建日程 → 切换账号 B → 创建日程 → 切回账号 A → 只看到账号 A 的日程 ✅
- [x] **C57**: 完整流程：发送消息（含图片）→ 流式接收 → 审批提案 → 关闭面板 → 重新打开 → 对话历史完整 ✅
- [x] **C58**: 完整流程：深度规划对话 → 关闭应用 → 重新打开 → 深度规划历史完整恢复 ✅
- [x] **C59**: 完整流程：离线 → 查看/编辑日程 → 恢复网络 → AI 功能恢复正常 ✅
- [x] **C60**: 完整流程：Python 后端崩溃 → 自动重启 → 前端感知到状态变化 → AI 功能恢复 ✅